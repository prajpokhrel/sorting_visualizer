import React from "react";
import "./SortVisualizer.css";

const SortVisualizer = () => {

    const listOfBars = {};

    return (
    // will call other functions with all the required props
    // other function will call <Bar /> with all the visualization elements.
    <div className="SortVisualizer">
        <h1>Hello World</h1>
    </div>
    );
};

export default SortVisualizer;